<% if signed_in? && current_user.teacher? %>
  <div class="alert alert-success" role="alert">Logged in as teacher.</div>
<% end %>


<% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>" role="alert">
          <%= value %>
  </div>
<% end %>
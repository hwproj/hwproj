<h2>Полезные ссылки</h2>
<ul>
	<% @links.each do |link| %>
		<li><%= link_to link.name, "http://#{link.url}" %></li>
	<% end %>
</ul>
<br>
<% if signed_in? && current_user.teacher? %>
	<%= link_to "Добавить", new_link_path, class: "btn btn-primary" %>
<% end %>